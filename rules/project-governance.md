# CTXRoute rules

Active CTXRoute file rules live in `.claude/hooks/docs/`, the canonical location
used by the upstream corpus linter and source labels.

Add project-specific file rules there and validate them with
`npm run validate:ctxroute`. Keep this document as a short location guide; it is
not part of the injected corpus.

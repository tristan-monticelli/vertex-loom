# Claude instructions

`AGENTS.md` is the authoritative repository doctrine. Read it and the complete
user request before acting, then follow its initialization, development, audit,
documentation, and Git requirements.

Project-specific routed rules live in `.claude/hooks/docs/` and supplement,
rather than replace, `AGENTS.md`.
